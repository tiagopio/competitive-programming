#include <bits/stdc++.h>
using namespace std;

#define int long long

void solve() {
    string a, s, c;
    cin >> a >> s >> c;
    cout << 'A' << char(toupper(s[0])) << 'C' << '\n';
}

int32_t main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int t = 1;
    // cin >> t;
    while(t--) {
        solve();
    } 
    return 0;
}